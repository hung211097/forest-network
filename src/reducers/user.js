// import {actionTypes} from '../constants/actionType'

const initialState = {
  infoUser: null
}

export default (state = initialState, action) => {
  switch(action.type){
    default:
      return state
  }
}
